# better-auth declares optional framework peers (@sveltejs/kit, solid-js, etc.)
# that drag in vite/svelte. This is a Fastify backend — it imports none of them.
# legacy-peer-deps stops npm's strict resolver from trying to satisfy that
# irrelevant optional-peer graph (which otherwise ERESOLVE-fails on vite versions).
legacy-peer-deps=true
